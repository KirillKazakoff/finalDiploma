// import { useAppDispatch } from '../../../../redux/reduxHooks';
// import { setError } from '../../../../redux/slices/searchWaySlice';

// const useValidateCity = () => {
//     const dispatch = useAppDispatch();

//     return (inputEl: HTMLInputElement, stateCity: string) => {
//         const { value, name } = inputEl;

//         console.log(value, stateCity, name);
//         if (value && stateCity !== value) {
//             console.log('yes');
//             dispatch(setError({ name, error: 'customError' }));
//         }
//     };
// };

// export default useValidateCity;
